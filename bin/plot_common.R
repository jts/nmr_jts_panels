# generated by copilot
add_ordinal <- function(df, colname, levels) {
  df[["ordinal"]] <-
    as.integer(factor(df[[colname]], levels = levels, ordered = TRUE)) - 1L
  df
}

etl <- function(df, colname, levels) {
  df <- add_ordinal(df, colname, levels)
  df
}